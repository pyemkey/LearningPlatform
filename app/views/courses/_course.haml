%tr 
  %td= course.title
  %td= course.author.name
  %td= course.lessons.count
  %td= ""
  %td 
    - if course.isEnrolled?(current_user)
      = link_to "Resume", course, class: "btn btn-success btn-xs"
    - else 
      = link_to "Check", course , class: "btn btn-primary btn-xs"